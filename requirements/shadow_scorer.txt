# Shadow scorer service — v1.1 Slice A. Torch/transformers/tokenizers use the
# same coupled pins as requirements/api.txt (bump only together). torch is
# installed FIRST from the CPU index in docker/shadow_scorer.Dockerfile; the pin
# below is then already satisfied (the default Linux wheel bundles CUDA).
transformers==5.13.1          # EXACT: v5 ships breaking changes in minor releases; requires torch>=2.4
tokenizers==0.22.2            # EXACT: transformers 5.13.1 caps <=0.23.0; 0.23.0 final was never published, 0.23.1 violates the cap
torch==2.13.0                 # install from CPU index (Dockerfile)
redis~=8.0.1
psycopg[binary]~=3.3.4
prometheus-client~=0.25.0
pydantic-settings~=2.14.2
