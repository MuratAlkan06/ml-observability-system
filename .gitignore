# Python bytecode
__pycache__/
*.py[cod]

# Packaging / build artifacts
dist/
build/
*.egg-info/

# Virtual environments
venv/
.venv/

# Test & lint caches
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Environment files (real secrets ignored; the committed template is not)
.env
.env.*
!.env.example

# Terraform local working state (infra/) — provider cache, any state that ends
# up on disk, saved plans, and the tfvars file carrying the SSH ingress CIDR.
# The lock file .terraform.lock.hcl is NOT ignored: it is committed on purpose
# so CI and local runs resolve identical provider builds.
.terraform/
*.tfstate*
*.tfplan
terraform.tfvars

# IDE / editor
.idea/
.vscode/

# OS
.DS_Store
